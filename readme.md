So you paste your token into the file and then type “python3 cfso.py <domain>”

Make sure openssl is installed (brew install openssl)

Make sure CloudFlare module is installed (pip3 install CloudFlare)
